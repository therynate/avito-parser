🕵️ Avito Profile Parser
Скрипт на Python для многопоточного парсинга объявлений с профилей на [Avito.ru](https://avito.ru), используя список ссылок и при необходимости прокси. Результаты сохраняются в файл.
------------------------------------------------------------------------------------

📌 Особенности
- Многопоточный парсинг с использованием `ThreadPoolExecutor`
- Поддержка прокси-серверов (`http`)
- Извлечение названий и цен объявлений
- Работа с HTML через `BeautifulSoup`
- Простой вывод в консоль и запись в файл `results.txt`

------------------------------------------------------------------------------------

🛠️ Как использовать
    1. Установите зависимости
    ```bash
    pip install httpx beautifulsoup4

    2.Подготовьте файлы
    profiles.txt — список ссылок на страницы пользователей Avito (по одной на строку).
    proxies.txt (опционально) — список https-прокси (один на строку, формат: ip:port или https://ip:port).

    3. Запустите скрипт
    ```bash
    python parser.py

------------------------------------------------------------------------------------

📝 Формат вывода
Результат сохраняется в results.txt и выглядит так:
Ссылка: https://www.avito.ru/user/123456

Название: iPhone 13 Pro
Цена: 70 000 ₽

Название: Ноутбук ASUS
Цена: 45 000 ₽

------------------------------------------------------------------------------------

⚠️ Важные замечания
Если прокси меньше, чем ссылок — часть запросов будет отправлена напрямую.
Прокси должны поддерживать https.
Структура Avito может меняться — тогда потребуется адаптировать BeautifulSoup-парсинг.

------------------------------------------------------------------------------------

📁 Структура проекта
.parser0000
-parser.py
-profiles.txt
-proxies.txt
-README.txt
-requirements.txt

------------------------------------------------------------------------------------

👨‍💻 Автор
[renat]
mail: renajok31@gmail.com